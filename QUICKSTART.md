# ⚡ БЫСТРЫЙ СТАРТ - FoodOrder

## 🎯 Для тех, кто уже установил PHP, Composer, Node.js и MySQL

### 1️⃣ Клонирование
```bash
git clone <URL_РЕПОЗИТОРИЯ> food-order
cd food-order
```

### 2️⃣ Автоматическая установка

**Linux/macOS:**
```bash
chmod +x install.sh
./install.sh
```

**Windows:**
```cmd
install.bat
```

### 3️⃣ Создайте БД вручную (перед запуском скрипта)
```sql
CREATE DATABASE food_order CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4️⃣ Настройте .env
```env
DB_DATABASE=food_order
DB_USERNAME=root
DB_PASSWORD=ваш_пароль
```

### 5️⃣ Запуск
```bash
php artisan serve
```

**Готово!** Откройте http://127.0.0.1:8000

---

## 👤 Вход

**Админ:**
- Телефон: `+375293709505`
- Пароль: `admin123`

**Пользователь:**
- Зарегистрируйтесь на сайте

---

## 📚 Полная инструкция

См. файл `INSTALLATION.md`

---

## 🆘 Проблемы?

1. **Ошибка БД:** Проверьте `.env` файл
2. **Не запускается:** `composer install && npm install`
3. **Страница не загружается:** `npm run build`
4. **Другие проблемы:** См. `INSTALLATION.md` раздел "Частые проблемы"

