# Настройка Telegram для уведомлений администраторов

## 📌 **Уведомления приходят в Telegram**

Telegram бот отправляет уведомления админам о новых заказах и изменениях статусов.

---

## 🚀 Быстрый старт

### 1. Создание бота

1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте **токен бота** (например: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Получение Chat ID админа(ов)

**Способ 1: Для одного админа**
1. Напишите боту любое сообщение
2. Откройте: `https://api.telegram.org/bot<ТОКЕН>/getUpdates`
3. Найдите: `"chat":{"id": <число>}` - это Chat ID

**Способ 2: Для нескольких админов**
1. Повторите шаг 1-3 для каждого админа
2. Соберите все Chat ID через запятую

### 3. Настройка переменных окружения

В файле `.env` добавьте:

```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=1234567890
```

**Если несколько админов:**
```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=1234567890,9876543210,5555555555
```

### 4. Тестирование

```bash
php artisan tinker
>>> app('TelegramService')->testBot()
```

Результат:
```
[
    "success" => true,
    "bot_name" => "FoodOrderBot",
    "bot_username" => "foodorder_bot",
]
```

---

## 📬 Какие уведомления отправляются?

### 🆕 Новый заказ
```
🆕 Новый заказ в FoodOrder

👤 Клиент: Иван Петров
📱 Телефон: +375 (29) 123-45-67
💰 Сумма: 45.50 BYN
🏠 Адрес: Минск, ул. Ленина, 1

📋 Детали: /admin/orders/42
```

### 👨‍🍳 Изменение статуса
```
👨‍🍳 Изменение статуса заказа #42

👤 Клиент: Иван Петров
📱 Телефон: +375 (29) 123-45-67
📊 Новый статус: Готовится

📋 Детали: /admin/orders/42
```

---

## 🔧 Технические детали

### Таблица telegram_users

```sql
CREATE TABLE telegram_users (
    id INTEGER PRIMARY KEY,
    phone VARCHAR(255) UNIQUE,
    chat_id VARCHAR(255),
    first_name VARCHAR(255),
    username VARCHAR(255),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

**Примечание:** Используется для истории уведомлений (опционально).

### Методы TelegramService

```php
// Отправить уведомление о новом заказе
sendNewOrderNotification(array $orderData): bool

// Отправить уведомление об изменении статуса
sendOrderStatusUpdate(array $orderData): bool

// Отправить сообщение админу(ам)
sendMessageToAdmin(string $message): bool

// Отправить в конкретный чат
sendMessageToChat(string $chatId, string $message): bool

// Проверить, работает ли бот
testBot(): array
```

---

## 🔒 Безопасность

- ✅ TELEGRAM_BOT_TOKEN хранится в `.env` (не в коде)
- ✅ Chat IDs защищены и видны только админам
- ✅ Все сообщения логируются
- ✅ API вызовы защищены timeout
- ✅ Ошибки логируются автоматически

---

## 🐛 Решение проблем

### Бот не отправляет сообщения
1. Проверьте токен в `.env`: `php artisan tinker` → `env('TELEGRAM_BOT_TOKEN')`
2. Проверьте Chat ID: `env('TELEGRAM_CHAT_ID')`
3. Убедитесь что админ хоть раз написал боту

### Нужно проверить логи
```bash
tail -f storage/logs/laravel.log | grep -i telegram
```

### Нужно отправить тестовое сообщение
```bash
php artisan tinker
>>> app('TelegramService')->sendMessageToAdmin('🧪 Тестовое сообщение')
```

---

## ✅ Статус

- ✅ Система полностью работает
- ✅ Поддержка нескольких админов
- ✅ Уведомления отправляются автоматически
- ✅ Готово к продакшену

