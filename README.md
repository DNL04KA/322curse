# 🍕 FoodOrder - Сервис заказа еды

Система для заказа еды из местных столовых и кафе Минска.

## 🚀 Быстрый запуск

### Шаг 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/yourusername/food-order.git
cd food-order
```

### Шаг 2️⃣ Установка зависимостей
```bash
composer install
npm install
```

### Шаг 3️⃣ Настройка окружения
```bash
cp .env.example .env
php artisan key:generate
```

### Шаг 4️⃣ Подготовка БД и миграции
**Вариант A: SQLite (по умолчанию, рекомендуется для разработки)**
```bash
php artisan migrate
```

**Вариант B: MySQL (для продакшена)**
```bash
# Отредактируйте .env:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=foodorder
# DB_USERNAME=root
# DB_PASSWORD=your_password

# Затем запустите миграции:
php artisan migrate
```

### Шаг 5️⃣ Сборка frontend (опционально)
```bash
# Для продакшена (one-time build)
npm run build

# Для разработки (с hot reload)
npm run dev
```

### Шаг 6️⃣ Запуск сервера
```bash
php artisan serve
```
Приложение будет доступно по адресу: **http://localhost:8000**

### Шаг 7️⃣ (Опционально) Telegram интеграция
```bash
# Создайте бота через @BotFather в Telegram
# Получите токен и добавьте в .env:
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_admin_chat_id_here

# Для нескольких администраторов (разделите запятыми):
TELEGRAM_CHAT_ID=admin_id_1,admin_id_2,admin_id_3

# Протестируйте подключение:
php artisan tinker
>>> app('TelegramService')->testBot()
```

## 📱 Использование

### Для пользователей:
1. **Регистрация**: Перейти на `/register` и заполнить форму
2. **Заказ еды**: Выберите ресторан → добавьте блюда в корзину → оформите заказ
3. **Отслеживание**: Отследите статус заказа на странице `/orders/track`
4. **История**: Авторизованные пользователи видят историю заказов

### Для администраторов:
1. **Вход**: Используйте любой аккаунт (автоматически получает права админа)
2. **Управление заказами**: Переходите в `/admin/orders`
3. **Изменение статуса**: Используйте AJAX для обновления статуса заказов
4. **Уведомления**: Автоматически получаете уведомления о новых заказах в Telegram

## 🔔 Telegram интеграция

Telegram используется **только** для отправки уведомлений администраторам о новых заказах и изменении статусов.

### Для администраторов ⚙️
1. Создайте телеграм бота через [@BotFather](https://t.me/BotFather)
2. Получите `TELEGRAM_BOT_TOKEN` 
3. Получите ваш Telegram ID:
   - Отправьте сообщение боту
   - Используйте [ссылку](https://api.telegram.org/bot<TOKEN>/getUpdates) с вашим токеном
   - Найдите `"id"` в ответе
4. Добавьте в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   TELEGRAM_CHAT_ID=123456789
   ```
5. Протестируйте:
   ```bash
   php artisan tinker
   >>> app('TelegramService')->testBot()
   ```

### Поддержка нескольких администраторов 👥
Разделите несколько chat ID запятыми:
```env
TELEGRAM_CHAT_ID=123456789,987654321,555666777
```

Подробнее в файле `TELEGRAM_SETUP.md`

## 🏗️ Архитектура

### Backend (PHP + Laravel):
- **MVC паттерн** - Модели, Представления, Контроллеры
- **Eloquent ORM** - Работа с базой данных
- **Middleware** - Аутентификация и авторизация
- **API** - RESTful endpoints

### Frontend (HTML + CSS + JS):
- **Blade шаблоны** - Серверный рендеринг
- **Bootstrap 5** - Адаптивный CSS фреймворк
- **JavaScript** - Валидация форм, AJAX запросы

### База данных:
- **SQLite** - Файловая база для разработки
- **Миграции** - Версионность схемы
- **Seeders** - Тестовые данные

## 📋 CRUDL операции

### Заказы (Orders):
- ✅ **Create**: `POST /orders` - Создание заказа
- ✅ **Read**: `GET /user/orders` - Просмотр заказов пользователя
- ✅ **Update**: `PATCH /admin/orders/{id}/status` - Изменение статуса
- ✅ **Delete**: Не реализовано (заказы не удаляются)
- ✅ **List**: `GET /admin/orders` - Список всех заказов

### Рестораны (Restaurants):
- ✅ **Create**: Через seeders
- ✅ **Read**: `GET /restaurants/{id}` - Детали ресторана
- ✅ **List**: `GET /` - Главная страница

### Корзина (Cart):
- ✅ **Create**: `POST /cart/add/{dish}` - Добавление товара
- ✅ **Update**: `PATCH /cart/update/{dish}` - Изменение количества
- ✅ **Delete**: `DELETE /cart/remove/{dish}` - Удаление товара
- ✅ **Clear**: `DELETE /cart/clear` - Очистка корзины

## 📱 Адаптивность

Приложение полностью адаптивно и работает на:
- ✅ **Мобильные** (< 576px)
- ✅ **Планшеты** (≥ 576px)
- ✅ **Десктопы** (≥ 768px)
- ✅ **Большие экраны** (≥ 992px)

## 🔐 Аутентификация

### Регистрация:
- Номер телефона + код из Telegram
- Email опционален
- Автоматическая верификация

### Вход:
- Только по номеру телефона
- Код подтверждения через Telegram

### Админ доступ:
- Все зарегистрированные пользователи имеют админ права
- Для продакшена нужно добавить поле `is_admin` в модель User

## 🛠️ Команды Artisan

```bash
# SMS + Telegram
php artisan sms:test +375291234567          # Тест SMS отправки
php artisan telegram:setup-webhook          # Настройка webhook бота
php artisan telegram:setup-webhook --remove # Удаление webhook
php artisan telegram:test-integration       # Тестирование Telegram

# База данных
php artisan migrate                         # Запуск миграций
php artisan db:seed                         # Заполнение тестовыми данными

# Кэш
php artisan cache:clear                     # Очистка кэша
php artisan config:clear                    # Очистка конфига
```

## 📊 Структура проекта

```
food-order/
├── app/                    # PHP код
│   ├── Http/Controllers/   # Контроллеры
│   ├── Models/            # Модели
│   └── Services/          # Сервисы
├── resources/views/       # Blade шаблоны
├── routes/               # Маршруты
├── database/             # Миграции и сидеры
├── public/               # Статические файлы
└── storage/              # Логи и кэш
```

## 🧪 Тестирование

```bash
# Запуск тестов
php artisan test

# Создание тестового пользователя
php artisan tinker
>>> User::create(['name'=>'Test','phone'=>'+375291234567','password'=>Hash::make('123')])

# Быстрый заказ (для тестирования)
# 1. Авторизуйтесь
# 2. Добавьте товары: /add-test-item
# 3. Создайте заказ с любыми данными
```

## 🚀 Продакшн

### Переменные окружения (.env):
```env
APP_NAME="FoodOrder"
APP_ENV=production
APP_KEY=base64_key_here
APP_DEBUG=false
APP_URL=https://yourdomain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=foodorder
DB_USERNAME=user
DB_PASSWORD=password

TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_admin_chat_id
```

### Оптимизация:
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
npm run build
```

## 📖 API документация

### Основные endpoints:

```
GET    /                    # Главная страница
GET    /restaurants/{id}    # Ресторан
GET    /cart               # Корзина
POST   /orders             # Создание заказа
GET    /user/orders        # Заказы пользователя
GET    /admin/orders       # Админ панель (все заказы)
```

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE

---

**Разработано для курса "Сервис заказа еды"** 🎓