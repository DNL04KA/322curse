# 🍽️ FoodOrder - ИНСТРУКЦИЯ ПО УСТАНОВКЕ

## 🎯 Для кого эта инструкция?

Эта инструкция написана простым языком для тех, кто хочет установить и запустить приложение **FoodOrder** на своем компьютере.

---

## 📋 Что нужно установить ПЕРЕД началом?

### 1. **PHP 8.2+**

**macOS:**
```bash
brew install php@8.2
```

**Linux:**
```bash
sudo apt update
sudo apt install php8.2 php8.2-mysql php8.2-mbstring php8.2-xml php8.2-curl
```

### 2. **Composer** (менеджер пакетов PHP)

Скачайте: https://getcomposer.org/download/

**Проверка установки:**
```bash
composer --version
```

### 3. **Node.js и npm** (для фронтенда)

Скачайте: https://nodejs.org/ (выбирайте LTS версию)

**Проверка установки:**
```bash
node --version
npm --version
```

### 4. **MySQL 8.0+** (база данных)

**Windows/macOS:**
- Скачайте MySQL Community Server: https://dev.mysql.com/downloads/mysql/

**macOS (через Homebrew):**
```bash
brew install mysql
brew services start mysql
```

**Linux:**
```bash
sudo apt install mysql-server
sudo systemctl start mysql
```

**Проверка:**
```bash
mysql --version
```

---

## 🚀 УСТАНОВКА ПРОЕКТА

### Шаг 1: Клонирование репозитория

```bash
# Перейдите в папку, где хотите разместить проект
cd ~/Desktop

# Клонируйте репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> food-order

# Перейдите в папку проекта
cd food-order
```

---

### Шаг 2: Установка зависимостей

```bash
# Установка PHP зависимостей
composer install

# Установка JavaScript зависимостей
npm install
```

⏱️ **Это займет 3-5 минут**

---

### Шаг 3: Настройка окружения

```bash
# Скопируйте файл с настройками
cp .env.example .env

# Сгенерируйте ключ приложения
php artisan key:generate
```

---

### Шаг 4: Настройка базы данных

#### 4.1 Создайте базу данных MySQL

**Вариант 1: Через командную строку**
```bash
# Войдите в MySQL (пароль может не потребоваться)
mysql -u root -p

# Создайте базу данных
CREATE DATABASE food_order CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# Выйдите
EXIT;
```

**Вариант 2: Через phpMyAdmin (если используете XAMPP)**
1. Откройте http://localhost/phpmyadmin
2. Нажмите "Новая" слева
3. Введите имя: `food_order`
4. Кодировка: `utf8mb4_unicode_ci`
5. Нажмите "Создать"

#### 4.2 Настройте файл .env

Откройте файл `.env` в любом текстовом редакторе и измените эти строки:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=food_order
DB_USERNAME=root
DB_PASSWORD=ваш_пароль_от_mysql
```

❗ **ВАЖНО:** Замените `ваш_пароль_от_mysql` на ваш реальный пароль MySQL

---

### Шаг 5: Создание таблиц и данных

```bash
# Создайте таблицы в базе данных
php artisan migrate

# Добавьте тестовые данные и админа
php artisan db:seed
```

✅ **После этой команды будет создан админ:**
- Телефон: `+375293709505`
- Пароль: `admin123`

---

### Шаг 6: Сборка фронтенда

```bash
# Соберите CSS и JavaScript файлы
npm run build
```

⏱️ **Это займет 1-2 минуты**

---

### Шаг 7: Запуск приложения

```bash
# Запустите локальный сервер
php artisan serve
```

🎉 **Готово! Приложение запущено!**

Откройте браузер и перейдите по адресу: **http://127.0.0.1:8000**

---

## 👤 ПЕРВЫЙ ВХОД

### Для обычного пользователя:

1. Нажмите "Войти" в правом верхнем углу
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя и фамилия
   - Телефон (например: +375291234567)
   - Email (опционально)
   - Пароль (минимум 8 символов)

### Для администратора:

1. Нажмите "Войти"
2. Введите данные админа:
   - **Телефон:** `+375293709505`
   - **Пароль:** `admin123`
3. После входа в меню появится "Админка"

---

## 🔧 ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ

### Telegram уведомления (необязательно)

Если хотите получать уведомления о новых заказах в Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Узнайте свой Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Добавьте в `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
```

5. Перезапустите сервер (Ctrl+C, затем снова `php artisan serve`)

---

## ❓ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "SQLSTATE[HY000] [1045] Access denied"

**Решение:** Неверный пароль MySQL в `.env`
```bash
# Проверьте пароль от MySQL
mysql -u root -p

# Если войдете успешно, обновите DB_PASSWORD в .env
```

### Проблема 2: "npm: command not found"

**Решение:** Node.js не установлен
```bash
# Скачайте и установите Node.js
# https://nodejs.org/
```

### Проблема 3: "composer: command not found"

**Решение:** Composer не установлен
```bash
# Скачайте и установите Composer
# https://getcomposer.org/download/
```

### Проблема 4: "Class "PDO" not found"

**Решение:** PHP без поддержки MySQL
```bash
# Установите расширение
# Windows (в php.ini раскомментируйте):
extension=pdo_mysql

# macOS/Linux:
sudo apt install php8.2-mysql
```

### Проблема 5: Страница не загружается после "npm run build"

**Решение:** Очистите кеш
```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

---

## 🎯 КАК ПОЛЬЗОВАТЬСЯ ПРИЛОЖЕНИЕМ?

### Для клиентов:

1. **Зарегистрируйтесь** или войдите
2. **Выберите ресторан** на главной странице
3. **Просмотрите меню** и добавьте блюда в корзину
4. **Оформите заказ** с указанием адреса доставки
5. **Отслеживайте статус** в разделе "Мои заказы"

### Для администратора:

1. **Войдите** с админ-аккаунтом
2. **Админка → Управление заказами** - посмотрите все заказы
3. **Измените статус** заказа (подтвердите, готовится, в пути, доставлен)
4. **Админка → Управление пользователями** - добавьте пользователей или назначьте админов

---

## 🛑 ОСТАНОВКА СЕРВЕРА

Нажмите **Ctrl + C** в терминале, где запущен `php artisan serve`

---

## 🔄 ОБНОВЛЕНИЕ ПРОЕКТА

```bash
# Получите последние изменения
git pull

# Обновите зависимости
composer install
npm install

# Примените новые миграции (если есть)
php artisan migrate

# Пересоберите фронтенд
npm run build

# Запустите сервер
php artisan serve
```

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не получается:

1. Проверьте, что все программы установлены (PHP, Composer, Node.js, MySQL)
2. Убедитесь, что MySQL запущен
3. Проверьте настройки в файле `.env`
4. Посмотрите логи в `storage/logs/laravel.log`

---

## 🎉 ПРИЯТНОГО ИСПОЛЬЗОВАНИЯ!

**Теперь у вас работает полноценный сервис заказа еды!**

❤️ Сделано с любовью для простоты использования

